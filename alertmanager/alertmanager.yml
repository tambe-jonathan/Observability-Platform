global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'sre-team-discord' # Or Slack/Email

receivers:
- name: 'sre-team-discord'
  # webhook_configs:
  #   - url: 'http://discord-notifier:8080/webhook'
